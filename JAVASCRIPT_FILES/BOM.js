// Browser Object Model (BOM) in JavaScript
// The BOM allows JavaScript to interact with the browser outside of the webpage content.
// It provides objects like window, navigator, screen, location, and history.

//  we can change html or access using the id, classname, tagname etc.
// The window object is the global object representing the browser window.
